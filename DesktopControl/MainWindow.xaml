<Window x:Class="DesktopControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DesktopControl"
        Title="Desktop Control" Height="650" Width="1100" Background="#1e1e2f">

    <Window.Resources>
        <local:StatusToColorConverter x:Key="StatusToColorConverter"/>
    </Window.Resources>

    <Grid x:Name="MainGrid" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderTitle" Text="💻 Desktop Control" FontSize="28" Foreground="White" FontWeight="Bold" Margin="10"/>

        <Border Grid.Row="1" Background="#2a2a40" CornerRadius="10" Padding="10">
            <DataGrid x:Name="ComputersGrid" AutoGenerateColumns="False" ItemsSource="{Binding Komputery}"
                      Background="#2a2a40" Foreground="White" GridLinesVisibility="None" RowBackground="#2a2a40"
                      AlternatingRowBackground="#33334d" BorderThickness="0" HeadersVisibility="Column" SelectionMode="Extended"
                      CanUserAddRows="False">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#000000"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="8"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="50">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="MAC Address" Binding="{Binding MAC}" Width="180" IsReadOnly="True"/>
                    <DataGridTextColumn Header="IP Address" Binding="{Binding IP}" Width="150" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Hostname" Binding="{Binding Hostname}" Width="180" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Online" Binding="{Binding Onl}" Width="80" IsReadOnly="True"/>

                    <DataGridTemplateColumn Header="Akcje" Width="280">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Włącz" Click="BtnPowerOn_Click" Background="#4CAF50" Foreground="White" Padding="8,3" Margin="0,0,5,0" BorderThickness="0"/>
                                    <Button Content="Wyłącz" Click="BtnPowerOff_Click" Background="#F44336" Foreground="White" Padding="8,3" Margin="0,0,5,0" BorderThickness="0"/>
                                    <Button Content="Podgląd" Background="#2196F3" Foreground="White" Padding="8,3" BorderThickness="0" Click="Preview_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Status / Błąd" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Error}" 
                                           Foreground="{Binding Error, Converter={StaticResource StatusToColorConverter}}" 
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap" VerticalAlignment="Center" Margin="5,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <WrapPanel x:Name="BottomActionsPanel" Grid.Row="2" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="BtnDetectComputers" Content="Wykryj komputery" Width="150" Background="#6a1b9a" Foreground="White" Padding="8" Margin="5" BorderThickness="0" Click="WykryjKomputery"/>
            <Button x:Name="BtnLoadFromFile" Content="Dodaj z pliku" Width="150" Background="#e67e22" Foreground="White" Padding="8" Margin="5" BorderThickness="0"/>
            <Border Width="30"/>
            <Button x:Name="BtnSelectAll" Click="BtnSelectAll_Click" Content="Zaznacz wszystko" Width="150" Background="#454566" Foreground="White" Padding="8" Margin="5" BorderThickness="0"/>
            <Button x:Name="BtnPowerOnAll" Content="Włącz wszystkie" Width="150" Background="#2e7d32" Foreground="White" Padding="8" Margin="5" BorderThickness="0"/>
            <Button x:Name="BtnPowerOffAll" Content="Wyłącz wszystkie" Width="150" Background="#c62828" Foreground="White" Padding="8" Margin="5" BorderThickness="0"/>
        </WrapPanel>
    </Grid>
</Window>