<Window x:Class="DesktopControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Desktop Control" Height="600" Width="1000" Background="#1e1e2f">

	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Text="💻 Desktop Control"
                   FontSize="28"
                   Foreground="White"
                   FontWeight="Bold"
                   Margin="10"/>

		<Border Grid.Row="1" Background="#2a2a40" CornerRadius="10" Padding="10">
			<DataGrid x:Name="ComputersGrid"
                      AutoGenerateColumns="False"
                      Background="#2a2a40"
                      Foreground="White"
                      GridLinesVisibility="None"
                      RowBackground="#2a2a40"
                      AlternatingRowBackground="#33334d"
                      BorderThickness="0"
                      HeadersVisibility="Column"
                      SelectionMode="Extended">

				<DataGrid.Resources>
					<Style TargetType="DataGridColumnHeader">
						<Setter Property="Background" Value="#000000"/>
						<Setter Property="Foreground" Value="White"/>
						<Setter Property="FontWeight" Value="Bold"/>
						<Setter Property="HorizontalContentAlignment" Value="Center"/>
						<Setter Property="Padding" Value="8"/>
					</Style>

					<Style TargetType="DataGridCell">
						<Setter Property="BorderThickness" Value="0"/>
						<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
						<Style.Triggers>
							<Trigger Property="IsSelected" Value="True">
								<Setter Property="Background" Value="Transparent"/>
								<Setter Property="Foreground" Value="White"/>
							</Trigger>
						</Style.Triggers>
					</Style>
				</DataGrid.Resources>

				<DataGrid.Columns>

					<DataGridTemplateColumn Width="50">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<CheckBox IsChecked="{Binding IsSelected}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>

					<DataGridTextColumn Header="MAC Address" Binding="{Binding Mac}" Width="200" IsReadOnly="True"/>

					<DataGridTextColumn Header="Broadcast IP" Binding="{Binding Ip}" Width="200" IsReadOnly="True"/>

					<DataGridTemplateColumn Header="Akcje" Width="300">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
									<Button Content="Włącz" Background="#4CAF50" Foreground="White" Padding="8,3" Margin="0,0,5,0"/>
									<Button Content="Wyłącz" Background="#F44336" Foreground="White" Padding="8,3" Margin="0,0,5,0"/>
									<Button Content="Podgląd" Background="#2196F3" Foreground="White" Padding="8,3"/>
								</StackPanel>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>

					<DataGridTemplateColumn Header="Status / Błąd" Width="*">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding Error}"
                                           Foreground="#ff5555"
                                           VerticalAlignment="Center"
                                           Margin="5,0"
                                           TextWrapping="Wrap"/>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>

				</DataGrid.Columns>
			</DataGrid>
		</Border>

		<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
			<Button Content="Zaznacz wszystko" Width="150" Background="#555" Foreground="White" Padding="8" Margin="5"/>
			<Button Content="Włącz wszystkie" Width="150" Background="#4CAF50" Foreground="White" Padding="8" Margin="5"/>
			<Button Content="Wyłącz wszystkie" Width="150" Background="#F44336" Foreground="White" Padding="8" Margin="5"/>
		</StackPanel>

	</Grid>
</Window>